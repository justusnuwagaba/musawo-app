




3. Modify the Login Component
Update the  Login  component to handle user roles after login. You will need to fetch the user's role from your authentication system (e.g., Firebase) after successful login.




4. Fetch User Role
You need to implement the  fetchUserRole  function to retrieve the user's role from your database. This can be done using Firestore or any other database you are using.

// Example function to fetch user role from Firestore
import { firestore } from './firebaseConfig'; // Ensure you have Firestore set up

const fetchUserRole = async (uid) => {
  const userDoc = await firestore.collection('users').doc(uid).get();
  if (userDoc.exists) {
    return userDoc.data().role; // Assuming you have a field named 'role'
  }
  throw new Error('User role not found');
};




have I defined the screens for  AdminPanel ,  DoctorDashboard , and the main patient screens in my navigation structure.?